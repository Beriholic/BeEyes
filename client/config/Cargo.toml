[package]
name = "config"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
models = { path = "../models" }
anyhow = "1.0.99"
toml = "0.9.5"
serde = { version = "1.0", features = ["derive"] }