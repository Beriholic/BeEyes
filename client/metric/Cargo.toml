[package]
name = "metric"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
models = { path = "../models" }
sysinfo = "0.37.0"
serde = { version = "1.0.219", features = ["derive"] }
chrono = "0.4.41"
